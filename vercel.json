{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "echo \"datasource db { provider = \\\"postgresql\\\"; url = env(\\\"DATABASE_URL\\\"); }\" > prisma/schema.prisma && prisma generate && next build",
  "devCommand": "prisma generate && next dev"
} 