{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "echo \"datasource db { provider = \\\"postgresql\\\"; url = env(\\\"DATABASE_URL\\\"); }\" > prisma/schema.prisma && prisma generate && SKIP_ESLINT=true next build",
  "devCommand": "prisma generate && next dev",
  "installCommand": "npm install",
  "postbuild": {
    "commands": [
      "npx prisma migrate deploy"
    ]
  }
} 